# Local k3d configuration for namespace-bootstrap.
# No secrets required. Initialized on first deploy by
# `just local-pulumi ...` or `just gcp-pulumi apply-namespaces --stack local`.
encryptionsalt: v1:f138zk61L/4=:v1:17qqtCjF7vFTUFIy:HeGtr5P/UR0U7//ShKMwkY0MCGQA1w==
config:
  namespace-bootstrap:properties:
    operatorNamespace: operators
    appNamespace: dev